# ScratKat examples

Run an example from the repository root after installing the project:

```bash
python examples/motion_on_start.py
```

The examples intentionally do not require image assets.  They demonstrate the
current sprite, script, motion, and startup-event APIs.
